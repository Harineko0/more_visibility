Contributing
============

This package only houses the annotation types for `more_visibility`.

- Keep scope minimal (annotations only).
- Use `dart format .` before sending changes.
- Run `dart analyze` or `dart test` if you add any sample code.

Release checklist
-----------------
- Update `CHANGELOG.md`.
- Bump `version` in `pubspec.yaml`.
